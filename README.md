## Jarvis AI Assistant

A voice-based AI assistant built using Python and Google Gemini API.

## Setup
1. Create a `.env` file
2. Install dependencies:
   pip install -r requirements.txt
3. Run:
   python free_jarvis.py
